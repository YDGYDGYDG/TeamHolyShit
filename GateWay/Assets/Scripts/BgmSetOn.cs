using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BgmSetOn : MonoBehaviour
{
    GameObject Bgm;

    // Start is called before the first frame update
    void Start()
    {
        Bgm = GameObject.Find("UI BGM");
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
